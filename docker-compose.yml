version: '3.8'

services:
  backend:
    build: ./backend
    container_name: expense-splitter-backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped